# Lattice v2: Semantic Memory
# These dependencies enable embedding-based semantic search.
# Without them, lattice falls back to grep-based search (still works).

sentence-transformers>=2.2.0
numpy>=1.24.0
fastsql>=2.1.0
pytest>=7.0.0
