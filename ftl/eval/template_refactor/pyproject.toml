[project]
name = "task-manager"
version = "0.1.0"
description = "Task manager for FTL campaign testing (refactoring scenario)"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=9.0.2"]
