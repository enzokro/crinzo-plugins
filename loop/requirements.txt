# Loop - Self-Learning Orchestrator
#
# Core dependencies (minimal)
# SQLite is built into Python, no external DB needed

# Semantic embeddings (optional but recommended)
# Enables meaning-based memory retrieval
sentence-transformers>=2.2.0

# For embedding computation
numpy>=1.21.0
